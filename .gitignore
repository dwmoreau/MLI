*.png
.ipynb_checkpoints
__pycache__
*weights.h5
*_base_line*
dist/
*.egg-info/

# Working notes for the ML-FOM project; not for GitHub.
docs/
.DS_Store

# profiler output (tools/profile_run.py)
indexing_profile/

# S07's fitted nulls: 131 MB of generated data, regenerable in one command by
# mlindex/scripts/run_fom_null.py. The directory is the deliverable's documented home, but the
# files themselves follow the standing rule that generated data is not committed -- as the
# benchmark pool and the FOM feature matrix already do.
mlindex/models/fom_null/
